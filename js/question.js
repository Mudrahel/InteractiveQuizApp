class Question {
    constructor(text, options, correctAnswerIndex) {
        this.text = text;
        this.options = options;
        this.correctAnswerIndex = correctAnswerIndex;
    }
}

// Export the Question class
export default Question;